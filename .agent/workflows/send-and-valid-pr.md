---
description: Create branch, commit, PR and merge
---

* Create a new branch based on main
* try to build application with npm run build command
* if the build succeed, create a commit with all modified files
* then push to the branch
* create a new PR
* merge the PR in squash mode
* delete the create branch
* back to the main branch
$ update main branch